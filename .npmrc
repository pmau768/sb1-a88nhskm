# Ensure package-lock.json is always generated and updated
package-lock=true

# Configure npm to use the exact versions specified in package.json
save-exact=true

# Always prefer offline cache when possible
prefer-offline=true

# Fetch registry data only when needed
offline=false

# Set default behavior for installing direct dependencies
save=true